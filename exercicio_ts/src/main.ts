function multiplicação(numeroA: number, numeroB: number): number {
    return numeroA * numeroB
}

console.log(multiplicação(5,6))

function saudacoes(nome: string): void {
    console.log(`Olá ${nome}`)
}

saudacoes('Breno')